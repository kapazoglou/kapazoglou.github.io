source "https://rubygems.org"

# Run `bundle install`, then `bundle exec jekyll serve` or `npm start`.
gem "jekyll", "~> 4.3"

group :jekyll_plugins do
    gem 'jekyll-feed'
    gem 'jekyll-sitemap'
    gem 'jekyll-paginate'
    gem 'jekyll-seo-tag'
end
---
module: settings
layer: logic
v: 2.6
date: 2026-07-19
deps: []
---
# Settings

v2: `nDice`, `nRoll`, `nPlace`, `nPlaces` steppers (`nPlaces` ≤ `nDice`, `nRoll` ≤ `nDice`, `nPlace` ≤ `nRoll`); `adjacentColumnsOnly`, `oneToOne`, `suitRestriction`, `consecutiveStars`, `stackBottomUp`, `fastAnimations` toggles. Persisted to localStorage.

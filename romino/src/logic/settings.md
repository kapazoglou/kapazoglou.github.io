---
module: settings
layer: logic
v: 2.10
date: 2026-07-20
deps: []
---
# Settings

v2: `nDice`, `nRoll`, `nPlace`, `nPlaces` steppers (`nPlaces` ≤ `nDice`, `nRoll` ≤ `nDice`, `nPlace` ≤ `nRoll`); `oneToOne`, `suitRestriction`, `consecutiveStars`, `tricolors`, `jokerFlushOnly`, `stackBottomUp`, `directPlacement`, `fastAnimations` toggles. Persisted to localStorage.
